<%= form_for(@situacao_avaliacao) do |f| %>
  <% if @situacao_avaliacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@situacao_avaliacao.errors.count, "error") %> prohibited this situacao_avaliacao from being saved:</h2>

      <ul>
      <% @situacao_avaliacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
